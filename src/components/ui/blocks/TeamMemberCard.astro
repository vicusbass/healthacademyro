---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

const { name, details, image } = Astro.props;

interface Props {
  name: string;
  details: string;
  image: string;
}

const images = import.meta.glob<{ default: ImageMetadata }>(
  "/src/images/team/*.{jpeg,jpg,png,gif}"
);
if (!images[image])
  throw new Error(
    `"${image}" does not exist in glob: "src/assets/*.{jpeg,jpg,png,gif}"`
  );
---

<div class="dark:bg-slate-800 flex items-center justify-center gap-6">
  <div
    class="group relative transform overflow-hidden rounded-xl bg-gray-100 p-5 shadow-xl transition-all duration-500 hover:shadow-2xl dark:bg-gray-700"
  >
    <div class="flex items-center gap-4">
      <!-- <img
        src={image}
        class="h-32 w-32 transform rounded-full object-cover object-center transition-all delay-500 duration-500 group-hover:h-36 group-hover:w-36"
      /> -->

      <Image
        src={images[image]()}
        alt={name}
        class="h-32 w-32 transform rounded-full object-cover object-center transition-all delay-500 duration-500 group-hover:h-36 group-hover:w-36"
      />
      <div class="w-fit transform transition-all duration-500">
        <h1 class="font-bold text-gray-600 dark:text-gray-200">{name}</h1>
        <p class="text-gray-400">{details}</p>
      </div>
    </div>
  </div>
</div>
