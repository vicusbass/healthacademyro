---
import { Image } from 'astro:assets';
// import heroImageSrc from '../assets/hero-image.avif';
import heroImageSrc from '../assets/hero1.jpeg';
---
<section class="relative pt-32 pb-20 md:pt-40 overflow-hidden hero-section">
  <div class="container-custom relative z-10">
    <div class="grid md:grid-cols-2 gap-16 items-center">
      <div class="slide-up" transition:animate="slide">
        <h1 class="mb-6 text-secondary-900 dark:text-white leading-tight">
           Recuperare medicală cu <span class="text-red-500 dark:text-red-500">Health Academy</span>
        </h1>
        <p class="text-xl text-secondary-600 dark:text-secondary-300 mb-8 max-w-lg">
          Centru de excelență în recuperare și performanță sportivă<br>
          Centru de excelență în ortopedie
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/contact" class="btn-primary">
            Contacteaza-ne
          </a>
          <a href="/servicii" class="btn-outline flex items-center">
            Afla mai multe
          </a>
        </div>
      </div>
      
      <div class="relative fade-in w-full h-full" transition:animate="fade">
        <div class="w-full h-full flex items-center justify-center">
          <Image 
            src={heroImageSrc} 
            alt="Hero Image" 
            class="w-full h-auto object-contain rounded-2xl shadow-2xl transform hover:scale-105 transition-transform duration-300"
            width={600}
            height={450}
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Add keyframes for additional animations if needed */
  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
  }
  
  /* Apply floating animation to the entire SVG container */
  .fade-in svg {
    animation: float 6s ease-in-out infinite;
  }
</style>
